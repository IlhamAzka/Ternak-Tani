import 'package:flutter/material.dart';

class SektorCard extends StatefulWidget {
  const SektorCard({ Key? key }) : super(key: key);

  @override
  _SektorCardState createState() => _SektorCardState();
}

class _SektorCardState extends State<SektorCard> {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}

class SektorMenuCard extends StatefulWidget {
  const SektorMenuCard({ Key? key }) : super(key: key);

  @override
  SektorMenuCardState createState() => SektorMenuCardState();
}

class SektorMenuCardState extends State<SektorMenuCard> {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}
